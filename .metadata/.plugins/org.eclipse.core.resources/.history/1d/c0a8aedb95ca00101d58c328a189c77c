spring.application.name=Visit-Service
# ------------------------------------
# Application Info
# ------------------------------------
server.port=8085

# ------------------------------------
# PostgreSQL Database (Supabase Pgbouncer)
# Use pooled connection for application
# ------------------------------------
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true
spring.datasource.username=postgres.vngopvvdofpbeglugxdk
spring.datasource.password=[YOUR-PASSWORD]
spring.datasource.driver-class-name=org.postgresql.Driver

# ------------------------------------
# Hikari Connection Pooling
# Recommended for Supabase
# ------------------------------------
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# ------------------------------------
# Hibernate / JPA
# ------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ------------------------------------
# Flyway / Liquibase (if needed)
# Note: you DID NOT request migrations here, so disabled
# ------------------------------------
spring.flyway.enabled=false

# ------------------------------------
# Eureka (IF your project uses service discovery)
# If NOT using Eureka, delete this block
# ------------------------------------
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
